import { Typography } from 'antd';

export function SettingsPage() {
  return (
    <Typography.Title level={3} style={{ padding: 24 }}>
      Settings — coming soon (F11)
    </Typography.Title>
  );
}
