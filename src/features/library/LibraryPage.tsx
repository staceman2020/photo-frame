import { Typography } from 'antd';

export function LibraryPage() {
  return (
    <Typography.Title level={3} style={{ padding: 24 }}>
      Library — coming soon (F4)
    </Typography.Title>
  );
}
