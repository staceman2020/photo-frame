import { Typography } from 'antd';

export function LayoutsPage() {
  return (
    <Typography.Title level={3} style={{ padding: 24 }}>
      Layout designer — coming soon (F9)
    </Typography.Title>
  );
}
