import { Typography } from 'antd';

export function FiltersPage() {
  return (
    <Typography.Title level={3} style={{ padding: 24 }}>
      Filters — coming soon (F8)
    </Typography.Title>
  );
}
