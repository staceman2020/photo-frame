import { Typography } from 'antd';

export function SlideshowPage() {
  return (
    <Typography.Title level={3} style={{ padding: 24 }}>
      Slideshow — coming soon (F10)
    </Typography.Title>
  );
}
